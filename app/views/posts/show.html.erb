<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    </head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-9">
                <h1 class="text-center">
                    <%= @post.title %>
                </h1>
                <hr>
                <p class="text-center">
                    <%= @post.content %>
                </p>
                <hr>
                <div class="text-center">
                    <%= button_to "Deletar Postagem", @post, method: :delete, data: {turbo_confirm: "Tem certeza que deseja deletar esta postagem?"}, class: "btn btn-outline-danger" if authenticated?%>
                </div>
                <% if authenticated?%>
                    <hr>
                <% end %>
                <div class="text-center">
                    <%= link_to "Voltar", posts_path, class: "btn btn-outline-warning me-2"%>
                    <%= link_to "Editar", edit_post_path(@post), class:"btn btn-outline-light me-2" if authenticated?%>
                    <%= link_to "Acessar Projeto", @post.link, class: "btn btn-outline-info me-2" if @post.link? %>

                </div>
            </div>
            
        </div>
        
    </div>
    
</body>
</html>
    


